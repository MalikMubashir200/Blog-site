<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Your Post | DailyScroll</title>
    <!-- links start here -->
    <link rel="stylesheet" href="/Style/output.css">
    <link rel="shortcut icon" href="/Images/favicon-16x16.png" type="image/png">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Arima:wght@100..700&family=Poppins:wght@100;400;700&display=swap" rel="stylesheet">
    <!-- links end here -->
    <style>
        body {
            font-family: "Poppins", sans-serif;
        }
    </style>
</head>
<body class="bg-[#ecf0f1] h-screen" style="background: url(/Images/form-bg.jpg) center/cover fixed;">

    <!-- Title and Description -->
    <div class="text-center mb-6">
        <h2 class="text-[2.5rem] font-bold text-gray-700">Edit Your Blog Post</h2>
        <p class="text-gray-600">Fill in the details below to update your post.</p>
    </div>

    <!-- Form Container -->
    <div class="flex items-center justify-center">
        <div class="w-[95%]  md:[55%] lg:w-[45%]  bg-white shadow-lg p-8 mx-4">
            <form class="space-y-6" action="/update/<%= Post.id %>" method="post" >
                <!-- Blogger Name -->
                <div>
                    <label class="block text-gray-700 font-semibold mb-1" for="authorName">Blogger Name</label>
                    <input type="text" id="authorName" name="authorName" placeholder="Enter your name"
                        class="w-full border-b border-gray-400 focus:border-red-500 focus:outline-none py-2 text-lg"
                        value="<%= Post.Blogger %>" required>
                </div>
                <!-- Category -->
                <div>
                    <label class="block text-gray-700 font-semibold mb-1" for="category">Category</label>
                    <select id="category" name="category"
                        class="w-full border-b border-gray-400 focus:border-red-500 focus:outline-none py-2 text-lg" required>
                        <option value="" disabled>Select a category</option>
                        <option value="travel" <%= Post.Catogorie === 'travel' ? 'selected' : '' %>>Travel</option>
                        <option value="tech" <%= Post.Catogorie === 'tech' ? 'selected' : '' %>>Tech</option>
                        <option value="lifestyle" <%= Post.Catogorie === 'lifestyle' ? 'selected' : '' %>>Lifestyle</option>
                    </select>
                </div>

                <!-- Image Upload -->
                <div>
                    <label class="block text-gray-700 font-semibold mb-1" for="image">Image</label>
                    <!-- Display current image if it exists -->
                    <% if (Post.Image) {%>
                        <div class="mb-3">
                            <img src="<%= Post.Image %>" alt="Current image" class="w-32 h-auto">
                        </div>
                    <% } %>
                    <input type="file" id="image" name="image"
                        class="w-full border-b border-gray-400 focus:border-red-500 focus:outline-none py-2 text-lg">
                </div>

                <!-- Article Title -->
                <div>
                    <label class="block text-gray-700 font-semibold mb-1" for="title">Article Title</label>
                    <input type="text" id="title" name="title" placeholder="Enter article title"
                        class="w-full border-b border-gray-400 focus:border-red-500 focus:outline-none py-2 text-lg"
                        value="<%= Post.Title %>" required>
                </div>

                <!-- Article Content -->
                <div>
                    <label class="block text-gray-700 font-semibold mb-1" for="content">Article</label>
                    <textarea id="content" name="content" rows="4" placeholder="Write your article here..."
                        class="w-full border-b border-gray-400 focus:border-red-500 focus:outline-none py-2 text-lg" required><%= Post.Content %></textarea>
                </div>

                <!-- Submit Button -->
                <div class="text-center">
                    <button type="submit" class="w-full bg-gray-600 text-white font-medium py-3 hover:bg-gray-700">
                        Update Post
                    </button>
                </div>
            </form>
        </div>
    </div>

</body>
</html>